export const metadata = {
  alternates: {
    canonical: "https://www.xtrememachines.in/pelletizing-line-compounding-line/",
  },
};

export default function Layout({ children }) {
  return children;
}
